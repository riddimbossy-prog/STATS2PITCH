{
  "name": "stats2pitch-fresh",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "refresh": "node scripts/refreshBoards.js",
    "test": "node --test",
    "check": "node --check server/config.js && node --check server/http.js && node --check server/apiFootball.js && node --check server/statsApi.js && node --check server/odds.js && node --check server/engine.js && node --check server/store.js && node --check server/refresh.js && node --check server/index.js && node --check scripts/refreshBoards.js && node --check public/app.js && npm test"
  },
  "engines": {
    "node": ">=20.19.0 <23"
  }
}
