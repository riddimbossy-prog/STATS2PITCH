const $=q=>document.querySelector(q),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))
const cfg=window.__STATS2PITCH_CONFIG__||{},base=String(cfg.supabaseUrl||'').replace(/\/+$/,''),anon=String(cfg.supabaseAnonKey||''),apiFn=String(cfg.functionName||'stats2pitch-api')
const token=()=>localStorage.getItem('s2p_admin_access_token')||''
const refreshToken=()=>localStorage.getItem('s2p_admin_refresh_token')||''
function apiUrl(path){return`${base}/functions/v1/${apiFn}${path}`}
function authUrl(path){return`${base}/functions/v1/stats2pitch-auth${path}`}
async function edge(url,opts={}){const headers={apikey:anon,...(opts.headers||{})};if(token())headers.Authorization=`Bearer ${token()}`;if(opts.body&&!headers['Content-Type'])headers['Content-Type']='application/json';const r=await fetch(url,{...opts,headers,cache:'no-store'}),b=await r.json().catch(()=>({}));if(!r.ok)throw new Error(b?.error||'Request failed');return b}
async function login(email,password){const b=await edge(authUrl('/login'),{method:'POST',body:JSON.stringify({email,password})});localStorage.setItem('s2p_admin_access_token',b.access_token||'');localStorage.setItem('s2p_admin_refresh_token',b.refresh_token||'');return b}
function showLogin(message=''){document.body.innerHTML=`<div class="admin-shell"><div class="admin-login admin-card"><img class="brand-img" src="/assets/stats2pitch-logo.png" alt="Stats2Pitch"><h2>Admin</h2>${message?`<p>${esc(message)}</p>`:''}<label>Email<input id="adminEmail" type="email" autocomplete="username"></label><label>Password<input id="adminPassword" type="password" autocomplete="current-password"></label><button id="adminLogin">Sign in</button></div></div>`;$('#adminLogin').onclick=async()=>{const btn=$('#adminLogin');btn.disabled=true;btn.textContent='Signing in…';try{await login($('#adminEmail').value,$('#adminPassword').value);location.reload()}catch(e){showLogin(e.message)} }}
function learningRows(rows){return(rows||[]).slice(0,20).map(r=>`<div class="admin-row"><strong>${esc(r.country)} · ${esc(r.league)} · ${esc(r.market.replaceAll('-',' '))}</strong><span>${r.sample}</span><span>${Number(r.winRate).toFixed(1)}%</span><span>${esc(r.gate)}</span><span>${r.ready?'Active':'Learning'}</span></div>`).join('')||'<p>No learning profiles yet.</p>'}
async function render(){try{const d=await edge(apiUrl('/admin/overview'));const s=d.performance?.summary||{};document.body.innerHTML=`<div class="admin-shell"><header><div class="brand-block"><img class="brand-img" src="/assets/stats2pitch-logo.png" alt="Stats2Pitch"><small>Private Admin</small></div><div class="header-actions"><button id="runRefresh">Run board refresh</button><button id="adminLogout">Sign out</button></div></header><section class="admin-card"><h2>System overview</h2><div class="admin-grid"><div><small>Saved boards</small><b>${d.snapshots||0}</b></div><div><small>30-day picks</small><b>${s.picks||0}</b></div><div><small>Settled wins</small><b>${s.won||0}</b></div><div><small>Success rate</small><b>${Number(s.winRate||0).toFixed(1)}%</b></div></div></section><section class="admin-card"><h2>Data health</h2><div class="admin-grid"><div><small>Football data</small><b>${d.health?.footballData?'OK':'OFF'}</b></div><div><small>Source fixtures</small><b>${d.health?.sourceFixtures||0}</b></div><div><small>Analyzed</small><b>${d.health?.analyzedFixtures||0}</b></div><div><small>Verified fixtures</small><b>${d.health?.statsVerifiedFixtures||0}</b></div></div></section><section class="admin-card"><h2>Latest raw support</h2><div class="admin-row"><b>Match / Pick</b><b>Home %</b><b>Away %</b><b>Odds</b><b>Banker</b></div><div class="admin-table">${(d.latestPicks||[]).map(p=>`<div class="admin-row"><strong>${esc(p.home)} vs ${esc(p.away)} · ${esc(p.displaySelection||p.selection)}</strong><span>${p.homeConsensus}%</span><span>${p.awayConsensus}%</span><span>${Number(p.odds).toFixed(2)}</span><span>${p.bankerApproved?'Yes':'No'}</span></div>`).join('')||'<p>No picks on the latest board.</p>'}</div></section><section class="admin-card"><h2>Learning profiles</h2><div class="admin-row"><b>Profile</b><b>Sample</b><b>Win %</b><b>Gate</b><b>Status</b></div><div class="admin-table">${learningRows(d.learning)}</div></section><section class="admin-card"><h2>Latest board</h2><pre>${esc(JSON.stringify({date:d.latest?.snapshot_date,generatedAt:d.latest?.generated_at,picks:d.latest?.payload?.bestPicks?.length||0,resultSummary:d.latest?.payload?.resultSummary||{}},null,2))}</pre><p id="adminStatus"></p></section></div>`;$('#adminLogout').onclick=()=>{localStorage.removeItem('s2p_admin_access_token');localStorage.removeItem('s2p_admin_refresh_token');location.reload()};$('#runRefresh').onclick=async()=>{const b=$('#runRefresh'),s=$('#adminStatus');b.disabled=true;b.textContent='Requesting…';try{const r=await edge(apiUrl('/admin/refresh'),{method:'POST',body:'{}'});s.textContent=r.message||'Refresh requested.'}catch(e){s.textContent=e.message}finally{b.disabled=false;b.textContent='Run board refresh'}}}catch(e){showLogin(e.message==='Admin access required'?'This account does not have admin access.':e.message)}}
if(!base||!anon)showLogin('Admin service is not configured.');else if(!token())showLogin();else render()
