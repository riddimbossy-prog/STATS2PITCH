export const app=document.getElementById('app')
export const state={config:null,user:null,board:null,date:null,tab:'best',market:'all',minFilters:1,refreshTimer:null,liveTimer:null}
export const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))
export const localToday=()=>{const d=new Date(),y=d.getFullYear(),m=String(d.getMonth()+1).padStart(2,'0'),day=String(d.getDate()).padStart(2,'0');return`${y}-${m}-${day}`}
export const getToken=()=>localStorage.getItem('s2p_access_token')||''
const getRefresh=()=>localStorage.getItem('s2p_refresh_token')||''
export const saveSession=x=>{if(x?.access_token)localStorage.setItem('s2p_access_token',x.access_token);if(x?.refresh_token)localStorage.setItem('s2p_refresh_token',x.refresh_token)}
export const clearSession=()=>{localStorage.removeItem('s2p_access_token');localStorage.removeItem('s2p_refresh_token')}
const normalizedBase=()=>String(state.config?.supabaseUrl||'').replace(/\/$/,'')
const apiTarget=path=>{if(!String(path).startsWith('/api/'))return path;const tail=String(path).slice(4),fn=state.config?.functionName||'stats2pitch-api';return`${normalizedBase()}/functions/v1/${fn}${tail}`}
async function refreshSession(){const r=getRefresh();if(!r||!state.config?.supabaseUrl)return false;const res=await fetch(`${normalizedBase()}/auth/v1/token?grant_type=refresh_token`,{method:'POST',headers:{apikey:state.config.supabaseAnonKey,'Content-Type':'application/json'},body:JSON.stringify({refresh_token:r})});if(!res.ok)return false;saveSession(await res.json());return true}
export async function api(path,opts={},retry=true){const headers={...(opts.headers||{}),apikey:state.config?.supabaseAnonKey||''};if(getToken())headers.Authorization=`Bearer ${getToken()}`;if(opts.body&&!headers['Content-Type'])headers['Content-Type']='application/json';const res=await fetch(apiTarget(path),{...opts,headers});if(res.status===401&&retry&&await refreshSession())return api(path,opts,false);const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error(data.error||`Request failed (${res.status})`);return data}
export async function loadConfig(){const cfg=globalThis.__STATS2PITCH_CONFIG__||{};const supabaseUrl=String(cfg.supabaseUrl||'').replace(/\/$/,'');const supabaseAnonKey=String(cfg.supabaseAnonKey||'');if(!supabaseUrl||!supabaseAnonKey)throw new Error('Stats2Pitch GitHub Pages deployment is missing Supabase runtime configuration');state.config={supabaseUrl,supabaseAnonKey,allowPublicSignup:cfg.allowPublicSignup!==false,functionName:cfg.functionName||'stats2pitch-api',version:cfg.version||'2.2.0'}}
export async function authRequest(kind,email,password){const signup=kind==='signup',url=signup?`${normalizedBase()}/auth/v1/signup`:`${normalizedBase()}/auth/v1/token?grant_type=password`;const res=await fetch(url,{method:'POST',headers:{apikey:state.config.supabaseAnonKey,'Content-Type':'application/json'},body:JSON.stringify({email,password})});const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error(data?.msg||data?.error_description||data?.message||'Sign in failed');if(data.access_token)saveSession(data);else if(data.session)saveSession(data.session);return data}
