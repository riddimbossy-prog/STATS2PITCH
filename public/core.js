export const app=document.getElementById('app')
export const state={config:null,user:null,board:null,date:null,tab:'best',market:'all',minFilters:1,refreshTimer:null,liveTimer:null}
export const esc=s=>String(s??'').replace(/[&<>'\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','\"':'&quot;'}[c]))
export const localToday=()=>{const d=new Date(),y=d.getFullYear(),m=String(d.getMonth()+1).padStart(2,'0'),day=String(d.getDate()).padStart(2,'0');return`${y}-${m}-${day}`}
export const getToken=()=>localStorage.getItem('s2p_access_token')||''
const getRefresh=()=>localStorage.getItem('s2p_refresh_token')||''
export const saveSession=x=>{if(x?.access_token)localStorage.setItem('s2p_access_token',x.access_token);if(x?.refresh_token)localStorage.setItem('s2p_refresh_token',x.refresh_token)}
export const clearSession=()=>{localStorage.removeItem('s2p_access_token');localStorage.removeItem('s2p_refresh_token')}
const normalizeSupabaseUrl=value=>{const raw=String(value||'').trim().replace(/\/+$/,'');if(!raw)return'';const dashboard=raw.match(/\/project\/([a-z0-9]+)/i)?.[1];if(dashboard)return`https://${dashboard}.supabase.co`;if(/^[a-z0-9]{15,30}$/i.test(raw))return`https://${raw}.supabase.co`;if(/^[a-z0-9-]+\.supabase\.co$/i.test(raw))return`https://${raw}`;return raw}
const normalizedBase=()=>normalizeSupabaseUrl(state.config?.supabaseUrl)
const apiTarget=path=>{if(!String(path).startsWith('/api/'))return path;const tail=String(path).slice(4),fn=state.config?.functionName||'stats2pitch-api';return`${normalizedBase()}/functions/v1/${fn}${tail}`}
const authTarget=path=>`${normalizedBase()}/functions/v1/stats2pitch-auth${path}`
async function edgeAuth(path,body){const res=await fetch(authTarget(path),{method:'POST',headers:{apikey:state.config.supabaseAnonKey,'Content-Type':'application/json'},body:JSON.stringify(body)});const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error('Unable to sign in right now');return data}
async function refreshSession(){const r=getRefresh();if(!r||!state.config?.supabaseUrl)return false;try{const data=await edgeAuth('/refresh',{refresh_token:r});if(!data?.access_token)return false;saveSession(data);return true}catch{return false}}
export async function api(path,opts={},retry=true){const headers={...(opts.headers||{}),apikey:state.config?.supabaseAnonKey||''};if(getToken())headers.Authorization=`Bearer ${getToken()}`;if(opts.body&&!headers['Content-Type'])headers['Content-Type']='application/json';const res=await fetch(apiTarget(path),{...opts,headers});if(res.status===401&&retry&&await refreshSession())return api(path,opts,false);const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error('Unable to load this right now');return data}
export async function loadConfig(){const cfg=globalThis.__STATS2PITCH_CONFIG__||{},supabaseUrl=normalizeSupabaseUrl(cfg.supabaseUrl),supabaseAnonKey=String(cfg.supabaseAnonKey||'').trim();if(!supabaseUrl||!supabaseAnonKey)throw new Error('Stats2Pitch is temporarily unavailable');try{new URL(supabaseUrl)}catch{throw new Error('Stats2Pitch is temporarily unavailable')};state.config={supabaseUrl,supabaseAnonKey,allowPublicSignup:cfg.allowPublicSignup!==false,functionName:cfg.functionName||'stats2pitch-api',version:cfg.version||'2.2.2'} }
export async function authRequest(kind,email,password){const signup=kind==='signup',data=await edgeAuth(signup?'/signup':'/login',{email,password});if(data?.access_token)saveSession(data);else if(data?.session)saveSession(data.session);return data}
