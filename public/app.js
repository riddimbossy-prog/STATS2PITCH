const root=document.getElementById('root')
const state={config:null,session:null,board:null,market:'ALL',minFilters:1,date:new Date().toISOString().slice(0,10)}
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))
const fmt=n=>Number.isFinite(Number(n))?Number(n).toFixed(2):'—'
const token=()=>state.session?.access_token||localStorage.getItem('mfa_access_token')||''

async function loadConfig(){state.config=await fetch('/api/config').then(r=>r.json())}
function saveSession(s){state.session=s; if(s?.access_token)localStorage.setItem('mfa_access_token',s.access_token); if(s?.refresh_token)localStorage.setItem('mfa_refresh_token',s.refresh_token)}
function clearSession(){state.session=null; localStorage.removeItem('mfa_access_token');localStorage.removeItem('mfa_refresh_token')}

async function supa(path,opts={}){
 const r=await fetch(state.config.supabaseUrl+path,{...opts,headers:{apikey:state.config.supabaseAnonKey,'Content-Type':'application/json',...(opts.headers||{})}})
 const j=await r.json().catch(()=>({})); if(!r.ok)throw new Error(j.msg||j.message||j.error_description||j.error||`Supabase ${r.status}`); return j
}
async function signIn(email,password){const j=await supa('/auth/v1/token?grant_type=password',{method:'POST',body:JSON.stringify({email,password})});saveSession(j);return j}
async function signUp(email,password){return supa('/auth/v1/signup',{method:'POST',body:JSON.stringify({email,password})})}
async function refreshSession(){const rt=localStorage.getItem('mfa_refresh_token');if(!rt)return null;try{const j=await supa('/auth/v1/token?grant_type=refresh_token',{method:'POST',body:JSON.stringify({refresh_token:rt})});saveSession(j);return j}catch{clearSession();return null}}
async function validate(){const t=token();if(!t)return false;let r=await fetch('/api/me',{headers:{Authorization:`Bearer ${t}`}});if(r.ok)return true;const n=await refreshSession();if(!n)return false;r=await fetch('/api/me',{headers:{Authorization:`Bearer ${token()}`}});return r.ok}

function absorbOAuthHash(){const p=new URLSearchParams(location.hash.slice(1));if(p.get('access_token')){saveSession({access_token:p.get('access_token'),refresh_token:p.get('refresh_token')});history.replaceState({},'',location.pathname+location.search)}}

function loginView(message=''){
 root.innerHTML=`<main class="login-shell"><section class="login-card"><div class="brand-mark">MF</div><p class="eyebrow">PRIVATE ACCESS</p><h1>Modular Football Agent</h1><p class="muted">Authorized users only. Real fixtures, enriched stats and strict modular classification.</p><form id="login-form"><label>Email<input id="email" type="email" required autocomplete="email"></label><label>Password<input id="password" type="password" minlength="6" required autocomplete="current-password"></label><button class="primary" type="submit">Sign in</button></form><button id="github" class="github">Continue with GitHub</button><p class="login-note">Accounts are managed in Supabase. Public signup is not exposed on this site.</p><p id="msg" class="status-msg">${esc(message)}</p></section></main>`
 const form=document.getElementById('login-form'),msg=document.getElementById('msg')
 form.onsubmit=async e=>{e.preventDefault();msg.textContent='Working…';try{const email=document.getElementById('email').value,password=document.getElementById('password').value;await signIn(email,password);await showDashboard()}catch(err){msg.textContent=err.message}}
 document.getElementById('github').onclick=()=>{const redirect=encodeURIComponent(location.origin);location.href=`${state.config.supabaseUrl}/auth/v1/authorize?provider=github&redirect_to=${redirect}`}
}

async function api(path,opts={}){const r=await fetch(path,{...opts,headers:{...(opts.headers||{}),Authorization:`Bearer ${token()}`}});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||`Request failed (${r.status})`);return j}
function groupRows(rows){return (rows||[]).filter(r=>r.filterCount>=state.minFilters&&(state.market==='ALL'||r.market===state.market))}
function table(title,subtitle,rows){rows=groupRows(rows);return `<section class="panel"><header class="panel-head"><div><h2>${esc(title)}</h2><p>${esc(subtitle)}</p></div><span class="count">${rows.length}</span></header>${rows.length?`<div class="table-wrap"><table><thead><tr><th>Match</th><th>League</th><th>Selected</th><th>Opponent</th><th>Odds</th><th>Draw</th><th>Filters</th><th>Reason</th></tr></thead><tbody>${rows.map(r=>`<tr><td><strong>${esc(r.match)}</strong><small>${esc(r.kickoffLocal)}</small></td><td><div class="entity">${r.countryFlag?`<img src="${esc(r.countryFlag)}" alt="" loading="lazy">`:''}<span>${esc(r.league)}<small>${esc(r.country)}</small></span></div></td><td><div class="entity">${r.selectedTeamLogo?`<img src="${esc(r.selectedTeamLogo)}" alt="" loading="lazy">`:''}<span>${esc(r.selectedTeam)} <span class="chip">#${esc(r.selectedPosition??'—')}</span></span></div></td><td><div class="entity">${r.opponentTeamLogo?`<img src="${esc(r.opponentTeamLogo)}" alt="" loading="lazy">`:''}<span>${esc(r.opponentTeam)} <span class="chip">#${esc(r.opponentPosition??'—')}</span></span></div></td><td class="num">${fmt(r.odds)}</td><td class="num">${fmt(r.drawOdds)}</td><td><strong>${r.filterCount}</strong><small>${esc(r.contradiction)} contradiction</small></td><td class="reason">${esc(r.shortReason)}</td></tr>`).join('')}</tbody></table></div>`:'<div class="empty">No fixtures currently qualify.</div>'}</section>`}
function renderBoard(){const b=state.board||{meta:{},groups:{},priority:[]},p=groupRows(b.priority).slice(0,12);root.innerHTML=`<main class="app-shell"><header class="topbar"><div><p class="eyebrow">MODULAR INTELLIGENCE</p><h1>Prediction Board</h1></div><div class="top-actions"><span class="user-pill">Login secured</span><button id="signout">Sign out</button></div></header><section class="toolbar"><div><label>Market<select id="market">${['ALL','1X2','O1.5','U1.5','O2.5','U2.5','O3.5','U3.5'].map(x=>`<option ${x===state.market?'selected':''}>${x}</option>`).join('')}</select></label></div><div><label>Minimum filters<select id="minf">${[1,2,3,5].map(x=>`<option value="${x}" ${x===state.minFilters?'selected':''}>${x}+</option>`).join('')}</select></label></div><div><label>Fixture date<input id="date" type="date" value="${esc(state.date)}"></label></div><div class="toolbar-spacer"></div><button id="reload">Reload snapshot</button><button id="refresh" class="primary compact">Refresh real data</button></section>${b.meta?.stale?`<div class="warning-banner">Showing last known good snapshot because the latest refresh failed.</div>`:''}<section class="metrics"><article><span>Fixtures scanned</span><strong>${b.meta?.fixturesScanned??0}</strong></article><article><span>Qualified picks</span><strong>${b.meta?.qualified??0}</strong></article><article><span>3+ filters</span><strong>${b.groups?.threePlus?.length??0}</strong></article><article><span>Last refresh</span><strong class="small-strong">${b.meta?.generatedAt?new Date(b.meta.generatedAt).toLocaleString():'No snapshot yet'}</strong></article></section><section class="panel"><header class="panel-head"><div><h2>Priority Prediction List</h2><p>Best to worst after contradiction checks</p></div></header><div class="priority-grid">${p.map((r,i)=>`<article class="pick-card"><div class="rank">#${i+1}</div><div><small>${esc(r.league)}</small><h3>${esc(r.selectedTeam)}</h3><p>${esc(r.match)}</p></div><div class="pick-stats"><strong>${fmt(r.odds)}</strong><span>${r.filterCount} filters</span><span>${esc(r.priorityLabel)}</span></div></article>`).join('')||'<div class="empty">No priority picks for these filters.</div>'}</div></section>${table('3+ Filters','Highest priority',b.groups?.threePlus)}${table('2 Filters','Strong combinations',b.groups?.two)}${table('Single Filter','Watchlist',b.groups?.single)}<footer class="footnote">Only verified API values are used. Missing data is skipped, never guessed. Successful boards are persisted in Supabase.</footer></main>`
 document.getElementById('signout').onclick=()=>{clearSession();loginView()};document.getElementById('market').onchange=e=>{state.market=e.target.value;renderBoard()};document.getElementById('minf').onchange=e=>{state.minFilters=Number(e.target.value);renderBoard()};document.getElementById('date').onchange=e=>{state.date=e.target.value};document.getElementById('reload').onclick=()=>loadBoard();document.getElementById('refresh').onclick=()=>refreshBoard()
}
async function loadBoard(){try{state.board=await api('/api/board');renderBoard()}catch(e){if(/Authentication/.test(e.message)){clearSession();loginView(e.message)}else alert(e.message)}}
async function refreshBoard(){const btn=document.getElementById('refresh');if(btn){btn.disabled=true;btn.textContent='Refreshing…'}try{state.board=await api(`/api/refresh?date=${encodeURIComponent(state.date)}`,{method:'POST'});renderBoard()}catch(e){alert(e.message);renderBoard()}}
async function showDashboard(){root.innerHTML='<div class="splash">Loading secured board…</div>';await loadBoard()}

(async()=>{try{await loadConfig();absorbOAuthHash();if(await validate())await showDashboard();else loginView()}catch(e){loginView(e.message)}})()
