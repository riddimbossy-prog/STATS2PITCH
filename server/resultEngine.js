import {num,odd,FAMILY,isTop3,isBottom3,familyStrength,families,contradiction} from './engineConfig.js'
import {pickBase,comparePicks} from './pickUtils.js'
const add=(a,code,label,direction,family,weight)=>a.push({code,label,direction,family,weight})
function signals(t,o,winOdd,drawOdd){const s=[],v=t.venue,ov=o.venue,tn=t.name,on=o.name;if(isTop3(t))add(s,'TOP3',`${tn} are Top 3 in the ${v.toUpperCase()} split`,'positive',FAMILY.TABLE,1.2);if(isBottom3(t))add(s,'BOTTOM3',`${tn} are Bottom 3 in the ${v.toUpperCase()} split`,'negative',FAMILY.TABLE,1.5);if(num(t.ppg)!==null){if(t.ppg>=2)add(s,'PPG_HIGH',`${tn} average at least 2.0 ${v} PPG`,'positive',FAMILY.TABLE,1.2);else if(t.ppg<1)add(s,'PPG_LOW',`${tn} average under 1.0 ${v} PPG`,'negative',FAMILY.TABLE,1.2)}if(num(t.goalsScored)!==null){if(t.goalsScored>=2.3)add(s,'GS_23',`${tn} score at least 2.3 per ${v} game`,'positive',FAMILY.ATTACK,1.3);else if(t.goalsScored>=2)add(s,'GS_20',`${tn} score at least 2.0 per ${v} game`,'positive',FAMILY.ATTACK,1.1);else if(t.goalsScored<1)add(s,'GS_LOW',`${tn} score under 1.0 per ${v} game`,'negative',FAMILY.ATTACK,1.1)}if(num(t.failedToScoreRate)!==null&&t.failedToScoreRate>=40)add(s,'FTS40',`${tn} failed to score in at least 40% of recent ${v} games`,'negative',FAMILY.ATTACK,1);if(num(t.goalsConceded)!==null){if(t.goalsConceded>2.3)add(s,'GC_23',`${tn} concede more than 2.3 per ${v} game`,'negative',FAMILY.DEFENCE,1.3);else if(t.goalsConceded>=2)add(s,'GC_20',`${tn} concede at least 2.0 per ${v} game`,'negative',FAMILY.DEFENCE,1.1);else if(t.goalsConceded<1)add(s,'GC_LOW',`${tn} concede under 1.0 per ${v} game`,'positive',FAMILY.DEFENCE,1.1)}if(num(t.cleanSheetRate)!==null&&t.cleanSheetRate>=60)add(s,'CS60',`${tn} kept clean sheets in at least 60% of recent ${v} games`,'positive',FAMILY.DEFENCE,.8);if(num(t.winRate)!==null){if(t.winRate>=80)add(s,'WIN80',`${tn} won at least 4 of the last 5 ${v} games`,'positive',FAMILY.FORM,1.4);else if(t.winRate>=60)add(s,'WIN60',`${tn} won at least 3 of the last 5 ${v} games`,'positive',FAMILY.FORM,1.1);else if(t.winRate<40)add(s,'WIN_LT40',`${tn} won fewer than 2 of the last 5 ${v} games`,'negative',FAMILY.FORM,1);else add(s,'WIN_LT60',`${tn} won fewer than 3 of the last 5 ${v} games`,'negative',FAMILY.FORM,.6)}if(num(t.lossRate)!==null){if(t.lossRate>=80)add(s,'LOSS80',`${tn} lost at least 4 of the last 5 ${v} games`,'negative',FAMILY.FORM,1.4);else if(t.lossRate>=60)add(s,'LOSS60',`${tn} lost at least 3 of the last 5 ${v} games`,'negative',FAMILY.FORM,1.1)}if(t.formAgreement==='WIN_STRONG')add(s,'FORM_WIN',`${tn}'s last 5 and 10 ${v} form agree strongly`,'positive',FAMILY.FORM,.7);if(t.formAgreement==='LOSS_STRONG')add(s,'FORM_LOSS',`${tn}'s poor ${v} form is confirmed across last 5 and 10`,'negative',FAMILY.FORM,.7);if(t.formAgreement==='CONFLICT')add(s,'FORM_CONFLICT',`${tn}'s last 5 and 10 ${v} form conflict`,'negative',FAMILY.FORM,1.2);if(odd(winOdd)!==null){if(winOdd<1.2)add(s,'ODDS120',`${tn} win price is below 1.20`,'positive',FAMILY.MARKET,1.5);else if(winOdd<=1.55)add(s,'ODDS155',`${tn} win price is 1.55 or lower`,'positive',FAMILY.MARKET,1.25);else if(winOdd<=2)add(s,'ODDS200',`${tn} win price is 2.00 or lower`,'positive',FAMILY.MARKET,.8);else if(winOdd>5)add(s,'ODDS500',`${tn} win price is above 5.00`,'negative',FAMILY.MARKET,1.4)}if(odd(drawOdd)!==null){if(drawOdd<3)add(s,'DRAW_LT3','Draw price is below 3.00','negative',FAMILY.MARKET,.8);else if(drawOdd>5)add(s,'DRAW_GT5','Draw price is above 5.00','positive',FAMILY.MARKET,1.1);else if(drawOdd>4)add(s,'DRAW_GT4','Draw price is above 4.00','positive',FAMILY.MARKET,.8)}if(isTop3(o))add(s,'OPP_TOP3',`${on} are Top 3 in the ${ov.toUpperCase()} split`,'negative',FAMILY.OPP,1.2);if(isBottom3(o))add(s,'OPP_BOTTOM3',`${on} are Bottom 3 in the ${ov.toUpperCase()} split`,'positive',FAMILY.OPP,1.2);if(num(o.ppg)!==null&&o.ppg<1)add(s,'OPP_PPG_LOW',`${on} average under 1.0 ${ov} PPG`,'positive',FAMILY.OPP,1.2);if(num(o.goalsScored)!==null&&o.goalsScored<1)add(s,'OPP_GS_LOW',`${on} score under 1.0 per ${ov} game`,'positive',FAMILY.OPP,1.1);if(num(o.goalsConceded)!==null){if(o.goalsConceded>2.3)add(s,'OPP_GC23',`${on} concede more than 2.3 per ${ov} game`,'positive',FAMILY.OPP,1.3);else if(o.goalsConceded>=2)add(s,'OPP_GC20',`${on} concede at least 2.0 per ${ov} game`,'positive',FAMILY.OPP,1.1);else if(o.goalsConceded<1)add(s,'OPP_GC_LOW',`${on} concede under 1.0 per ${ov} game`,'negative',FAMILY.OPP,1.1)}if(num(o.lossRate)!==null){if(o.lossRate>=80)add(s,'OPP_LOSS80',`${on} lost at least 4 of last 5 ${ov} games`,'positive',FAMILY.OPP,1.4);else if(o.lossRate>=60)add(s,'OPP_LOSS60',`${on} lost at least 3 of last 5 ${ov} games`,'positive',FAMILY.OPP,1.1)}if(num(o.failedToScoreRate)!==null&&o.failedToScoreRate>=40)add(s,'OPP_FTS40',`${on} failed to score in at least 40% of recent ${ov} games`,'positive',FAMILY.OPP,.8);if(num(o.cleanSheetRate)!==null&&o.cleanSheetRate>=60)add(s,'OPP_CS60',`${on} kept clean sheets in at least 60% of recent ${ov} games`,'negative',FAMILY.OPP,.8);return s}
function fallback(t,odds){const h=t.venue==='home',d=odd(h?odds.dnbHome:odds.dnbAway);if(d)return{market:'DNB',selection:`${t.name} DNB`,odds:d};const dc=odd(h?odds.dc1x:odds.dcx2);return dc?{market:'DC',selection:`${t.name} ${h?'1X':'X2'}`,odds:dc}:null}
function candidate(f,t,o,winOdd){if(!isTop3(t)||odd(winOdd)===null)return null;const s=signals(t,o,winOdd,f.odds.draw),pos=s.filter(x=>x.direction==='positive'),neg=s.filter(x=>x.direction==='negative');if(!pos.length)return null;const level=contradiction(pos,neg),ps=familyStrength(pos),ns=familyStrength(neg),fam=families(pos),score=fam.length*2.5+ps-ns*1.2+Math.max(0,2.5-winOdd)*.25;if(level==='HIGH')return null;let selection=t.name,market='1X2',price=odd(winOdd),safety='split-win-rate';const recent=num(t.winRate),season=t.played>=5?num(t.seasonWinRate):null,rate=recent??season,last=o.positionSampleReady===true&&o.played>=5&&num(o.position)===num(o.leagueSize),heavy=num(o.goalsConceded)!==null&&o.goalsConceded>2.3,safe=level==='LOW'&&((rate!==null&&rate>=60)||last||heavy);if(!safe){if(price<=2)return null;const fb=fallback(t,f.odds);if(!fb)return null;({market,selection,odds:price}=fb);safety=level==='MODERATE'?'moderate-downgrade':'under60-downgrade'}return pickBase(f,{market,selection,selectedTeamId:t.id,selectedTeamLogo:t.logo,odds:price,filterCount:pos.length,familyCount:fam.length,filterFamilies:fam,familyStrength:ps,negativeFamilyStrength:ns,contradiction:level,score:+score.toFixed(3),reasons:pos.map(x=>x.label),warnings:neg.map(x=>x.label),safety})}
export function resultPicks(f){const rows=[candidate(f,f.home,f.away,f.odds.home),candidate(f,f.away,f.home,f.odds.away)].filter(Boolean).sort(comparePicks);if(rows.length===1)return rows;if(rows.length>=2&&Math.abs(rows[0].score-rows[1].score)>=.75)return[rows[0]];return[]}
